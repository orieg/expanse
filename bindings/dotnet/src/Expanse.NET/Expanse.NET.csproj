<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>net8.0;net9.0</TargetFrameworks>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <LangVersion>latest</LangVersion>

    <!-- Package Metadata -->
    <PackageId>Orieg.Expanse</PackageId>
    <Title>Expanse.NET</Title>
    <Description>High-performance, cache-line-tuned modern Judy arrays and off-heap zero-GC associative collections for .NET 8.0 and .NET 9.0+ powered by libexpanse.</Description>
    <Version>0.3.0</Version>
    <PackageVersion>0.3.0</PackageVersion>
    <AssemblyVersion>0.3.0</AssemblyVersion>
    <Authors>Nicolas Brousse</Authors>
    <Company>Orieg</Company>
    <PackageLicenseExpression>MIT OR Apache-2.0</PackageLicenseExpression>
    <PackageProjectUrl>https://github.com/orieg/expanse</PackageProjectUrl>
    <RepositoryUrl>https://github.com/orieg/expanse.git</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>trie;map;bitset;blobmap;concurrency;lock-free;high-performance;native;interop</PackageTags>
    <PackageReadmeFile>README.md</PackageReadmeFile>

    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <IncludeSymbols>true</IncludeSymbols>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>
    <TreatWarningsAsErrors>false</TreatWarningsAsErrors>
    <NoWarn>$(NoWarn);CA2255;CS1591</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <None Include="..\..\README.md" Pack="true" PackagePath="\" Condition="Exists('..\..\README.md')" />
  </ItemGroup>

  <!--
    Native runtime libraries, staged by the release workflow into
    runtimes/{rid}/native/ before `dotnet pack`. The Exists() conditions keep the
    CI smoke-pack (which does not stage them) working; the release pack ships them.
  -->
  <ItemGroup>
    <None Include="runtimes\linux-x64\native\libexpanse.so" Pack="true" PackagePath="runtimes\linux-x64\native\" Condition="Exists('runtimes\linux-x64\native\libexpanse.so')" />
    <None Include="runtimes\osx-x64\native\libexpanse.dylib" Pack="true" PackagePath="runtimes\osx-x64\native\" Condition="Exists('runtimes\osx-x64\native\libexpanse.dylib')" />
    <None Include="runtimes\osx-arm64\native\libexpanse.dylib" Pack="true" PackagePath="runtimes\osx-arm64\native\" Condition="Exists('runtimes\osx-arm64\native\libexpanse.dylib')" />
    <None Include="runtimes\win-x64\native\expanse.dll" Pack="true" PackagePath="runtimes\win-x64\native\" Condition="Exists('runtimes\win-x64\native\expanse.dll')" />
  </ItemGroup>

</Project>
